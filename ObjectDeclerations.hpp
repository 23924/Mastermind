// this file contains the (enum) object declerations

#pragma once

inline Statemachine Statemachine; // Create Statemachine object containing the proccessing functions of the different states
inline Code myCode; // Create a code object containing the games code the user needs to crack

inline GameStates GameState; // Create enum object for the different gamestates
